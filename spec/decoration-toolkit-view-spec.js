'use babel';

import DecorationToolkitView from '../lib/decoration-toolkit-view';

describe('DecorationToolkitView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
